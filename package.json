{
  "name": "flux",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "esbuild": "^0.27.7"
  },
  "scripts": {
    "dev:docs": "npm run dev --workspace=apps/docs",
    "build:docs": "npm run build --workspace=apps/docs",
    "build:ui": "npm run check-types --workspace=packages/flux-ui"
  }
}
